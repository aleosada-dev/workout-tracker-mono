// Display names for library variations, keyed by the variation's camelCase slug
// (`variations.slug`). User-created variations have no slug and fall back to the
// stored `name` — see `resolveVariationName` in features/exercises/lib/format.
export default {};
