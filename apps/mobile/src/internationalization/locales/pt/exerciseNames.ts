// Display names for library exercises, keyed by the exercise's camelCase slug
// (`exercises.slug`). User-created exercises have no slug and fall back to the
// stored `name` — see `resolveExerciseName` in features/exercises/lib/format.
export default {};
