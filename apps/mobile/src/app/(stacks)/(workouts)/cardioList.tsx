import { Text } from '@workout-tracker/ui-mobile';
import { View } from 'react-native';

export default function CardioListScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text variant="h2">Cardio List</Text>
    </View>
  );
}
